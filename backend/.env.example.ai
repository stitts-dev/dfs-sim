# AI Integration Configuration
# Add these to your existing .env file

# Anthropic API Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# AI Service Configuration
AI_RATE_LIMIT=5                    # Max requests per minute per user
AI_CACHE_EXPIRATION=3600          # Cache duration in seconds (1 hour)

# Optional: Use different models for different purposes
# ANTHROPIC_MODEL=claude-3-haiku-20240307  # Default model (fast, cost-effective)
# ANTHROPIC_MODEL_ANALYSIS=claude-3-sonnet-20240229  # For detailed analysis (if needed)